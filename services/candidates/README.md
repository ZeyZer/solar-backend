# Candidate Services

This folder contains candidate generation and candidate-set orchestration, including filtering, cost and performance enrichment, ranking, shortlist construction, and the optimisation funnel policy.

`designCandidateSetService.js` is the main entry point for building the diagnostic candidate set returned by the quote routes.
